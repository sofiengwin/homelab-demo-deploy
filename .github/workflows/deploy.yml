name: Deploy Demo App To My Kubernetes Homelab
on: [push]
jobs:
  Test-SSH:
    runs-on: ubuntu-latest
    steps:
      - name: Test ssh connection
        run: |
            echo "This is a test"
            puts ${{ secrets.USERNAME }}
            puts ${{ secrets.HOST }}
            ssh ${{ secrets.USERNAME }}@${{ secrets.HOST }}


